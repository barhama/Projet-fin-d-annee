Projet-fin-d-annee
==================
il fallait modifier comme ça: il manquait le typdef
typedef struct clt
         {
                int codeclient;
                char* nom;
                char* prenom;
                char* adresse;
         }client;
